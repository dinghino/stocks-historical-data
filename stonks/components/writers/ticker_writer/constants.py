output_type = "INDIVIDUAL_TICKER_MULTIFILE_WRITER"
friendly_name = "Individual Ticker files"
description = """
Outputs one file for each {symbol:cyan}/{ticker:cyan}.

When selecting this output type usually {the symbol is removed:white|bold} from
the data since each file contains only one symbol.

"""  # noqa
