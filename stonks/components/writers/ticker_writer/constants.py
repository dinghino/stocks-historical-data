output_type = "INDIVIDUAL_TICKER_MULTIFILE_WRITER"
friendly_name = "Individual Ticker files"
description = """
Outputs one file for each symbol/ticker.

When selecting this output type usually the symbol is removed from
the data since each file contains only one symbol.

"""  # noqa
